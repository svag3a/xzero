services:
  app:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - scan_data:/data
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  scan_data:
